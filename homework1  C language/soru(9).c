#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int low, high, number, k, i;
	
	
	;printf("number:");
	scanf("%d", &number);
	
	;if(i>high)
{
	         high=i;
	         printf("high:%d", high);
}
	else 
{
	         k=low%number;
	         printf("k:%d", k);
}
	         
	if(k==0 && low==0)
{
	         low=low+1;
	         printf("low:%d", low);
}
	         
	else
{
		     printf("low:%d", low);
			 low=low+1;        
}
		     
	return 0;
}
